<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.bookreaderapp">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE" />
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.BookReaderApp">
        <activity android:name=".DeActivity"></activity>
        <activity android:name=".DbmsActivity" />
        <activity android:name=".CppActivity" />
        <activity android:name=".CcpActivity" />
        <activity android:name=".SeActivity" />
        <activity android:name=".PythonActivity" />
        <activity android:name=".OsActivity" />
        <activity android:name=".JavaActivity" />
        <activity android:name=".DsaActivity" />
        <activity android:name=".DaaActivity" />
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>